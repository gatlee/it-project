import React from 'react';
import { Button } from 'react-bootstrap';
const SignInButton = () => {
  return (
    <Button style={{ background: '#E5E5E5' }} variant="light">
      Sign In
    </Button>
  );
};

export default SignInButton;
